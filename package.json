{
  "name": "captains-log-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "sst test",
    "start": "sst start",
    "build": "sst build",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "devDependencies": {
    "@aws-cdk/assert": "1.94.1"
  },
  "dependencies": {
    "@aws-cdk/core": "1.94.1",
    "@serverless-stack/cli": "0.10.7",
    "@serverless-stack/resources": "0.10.7",
    "aws-sdk": "^2.878.0"
  }
}
